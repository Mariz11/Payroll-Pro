import React from 'react';

import EmployeeManualAttendance from 'lib/components/blocks/employeeManualAttendance';

const Page = ({ params }: { params: { companyName: string } }) => {
  return <>404</>
};

export default Page;
